<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Build" ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemGroup Label="ProjectConfigurations"><ProjectConfiguration Include="Release|Win32"><Configuration>Release</Configuration><Platform>Win32</Platform></ProjectConfiguration></ItemGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />
  <PropertyGroup Label="Configuration"><ConfigurationType>Utility</ConfigurationType></PropertyGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />
  <Import Project="..\common.props" />
  <Import Project="$(VCTargetsPath)\BuildCustomizations\masm.props" />
  <ItemDefinitionGroup><MASM><GenerateDebugInformation>false</GenerateDebugInformation><AdditionalOptions>/coff</AdditionalOptions><IncludePaths>$(MSBuildProjectDirectory);%(IncludePaths)</IncludePaths></MASM></ItemDefinitionGroup>
  <ItemGroup>
    <MASM Include="runtime.asm"><AdditionalDependencies>$(MSBuildProjectDirectory)\click.inc;%(AdditionalDependencies)</AdditionalDependencies></MASM>
    <None Include="click.inc" />
  </ItemGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
  <Import Project="$(VCTargetsPath)\BuildCustomizations\masm.targets" />
</Project>
